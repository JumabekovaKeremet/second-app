const Square=({ items })=>{
   items.forEach(item => {
       list.push(<tr {
           
       } catch (z) {
           
       }>{item}</tr>);
   });
return <ul>
    {list}
</ul>
}
export default Square;